SELECT YYYY,
    YYYY
FROM ( SELECT LINK_YR - LEVEL +2 AS LINK_YR /* 연계년도 */
FROM ( SELECT MAX(LINK_YR) AS LINK_YR /* 연계년도 */
FROM TBCSPSAE201M ) CONNECT BY TO_CHAR(SYSDATE,'YYYY') - LEVEL + 3 >= 2000 )