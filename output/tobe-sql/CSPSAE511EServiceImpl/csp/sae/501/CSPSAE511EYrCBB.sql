SELECT YYYY,
    YYYY
FROM ( SELECT LINK_YR - LEVEL +2 AS LINK_YR /* 연계년도 */
FROM ( SELECT TO_CHAR(SYSDATE, 'YYYY') AS LINK_YR /* 연계년도 */
FROM DUAL ) CONNECT BY TO_CHAR(SYSDATE,'YYYY') - LEVEL + 2 >= 2014 )