SELECT A.*
FROM ( SELECT A.LAW_SNO,
    A.STT_KDCD /* 법령종류코드 */,
    F_CODE_NM('1000288', A.STT_KDCD) AS F_CODE_NM,
    A.STT_NM /* 법령명 */,
    F_DPT_INFO(A.LAW_MNG_BRDV_CD, '1') AS CNSTN_MBCMT_DEPT_NM /* 자문위원부서명 */,
    A.LAW_SRT_CD,
    F_CODE_NM('1000289', A.LAW_SRT_CD) AS F_CODE_NM,
    A.LAW_ABRG_YN,
    (SELECT TO_CHAR(TO_DATE(MIN(Z.LAW_AMD_DT)), 'YYYY-MM-DD')
FROM TBCSPKDE002D Z WHERE Z.LAW_SNO = A.LAW_SNO) AS FRT_LAW_DT, (SELECT TO_CHAR(TO_DATE(MAX(Z.LAW_AMD_DT)), 'YYYY-MM-DD')
FROM TBCSPKDE002D Z WHERE Z.LAW_SNO = A.LAW_SNO) AS FNL_LAW_DT, B.LAW_AMD_DT, B.LAW_AMD_HST_TXT FROM TBCSPKDE001M A, ( SELECT *
FROM TBCSPKDE002D B WHERE (B.LAW_SNO, B.LAW_AMD_DT) IN( SELECT C.LAW_SNO,
    MAX(C.LAW_AMD_DT)
FROM TBCSPKDE002D C GROUP BY C.LAW_SNO) )B WHERE 1=1 AND A.LAW_SNO = B.LAW_SNO(+) ORDER BY A.LAW_SRT_CD, B.LAW_AMD_DT DESC ) A WHERE 1=1 AND A.STT_KDCD = #STT_KDCD# AND A.LAW_SRT_CD = #srtCd# AND A.FRT_LAW_DT &gt;= #frtDt1# AND A.FRT_LAW_DT &lt;= #frtDt2# AND A.LAW_AMD_DT &gt;= #fnlDt1# AND A.LAW_AMD_DT &lt;= #fnlDt2# AND A.STT_NM LIKE '%'|| #tit# ||'%'