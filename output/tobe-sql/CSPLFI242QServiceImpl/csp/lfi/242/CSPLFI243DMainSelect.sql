SELECT TOTAL_AM,
    PMT_SUM,
    APP_SUM,
    (TOTAL_AM - PMT_SUM - APP_SUM) AS TOTAL_AM
FROM ( SELECT RPAD(TOTAL_AM,9,'0') AS RPAD,
    (SELECT SUM(PMT_AM)
FROM TBCSPLFI242M WHERE LINK_YR = #LINK_YR# AND HNDL_STEP > 5) AS PMT_SUM , (SELECT SUM(PMT_AM)
FROM TBCSPLFI242M WHERE LINK_YR = #LINK_YR# AND HNDL_STEP = 3) AS APP_SUM FROM TBCSPLFI242D WHERE 1=1 AND LINK_YR = #LINK_YR# )T1