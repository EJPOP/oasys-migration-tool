SELECT T1.INST_CD /* 기관코드 */,
    T1.FRS_MDM_SQNO /* 포렌식매체순번 */,
    T1.KORN_INST_NM /* 한글기관명 */,
    T1.ENG_INST_NM /* 영문기관명 */,
    T1.WEB_SITE_ADDR /* 웹사이트주소 */,
    T1.CRU_TELNO /* 부패전화번호 */,
    T1.CRU_ZIP /* 부패우편번호 */,
    T1.OTSD_EXPRT_ADDR /* 외부전문가주소 */,
    (SELECT MIN(GCC_ORG_CD)
FROM TBCSPIIT006M WHERE BAI_ORG_CD = #INST_CD# AND ABLT_YN_CD = '0') AS GCC_ORG_CD ,(SELECT MIN(GCC_HHT_ORG_CD)
FROM TBCSPIIT006M WHERE BAI_ORG_CD = #INST_CD# AND ABLT_YN_CD = '0') AS GCC_HHT_ORG_CD FROM TBFAAAEA010M T1 WHERE T1.INST_CD = #INST_CD# AND T1.FRS_MDM_SQNO = (SELECT MAX(FRS_MDM_SQNO) AS FRS_MDM_SQNO /* 포렌식매체순번 */
FROM TBFAAAEA010M WHERE INST_CD = T1.INST_CD) UNION ALL SELECT INST_CD /* 기관코드 */,
    0 AS FRS_MDM_SQNO,
    PROF_RBPRSN_INST_NM AS PROF_RBPRSN_INST_NM /* 증명책임자기관명 */,
    '' AS ENG_INST_NM /* 영문기관명 */,
    '' AS WEB_SITE_ADDR /* 웹사이트주소 */,
    CRU_TELNO /* 부패전화번호 */,
    CRU_ZIP /* 부패우편번호 */,
    OTSD_EXPRT_ADDR /* 외부전문가주소 */,
    (SELECT MIN(GCC_ORG_CD)
FROM TBCSPIIT006M WHERE BAI_ORG_CD = #INST_CD# AND ABLT_YN_CD = '0') AS GCC_ORG_CD ,(SELECT MIN(GCC_HHT_ORG_CD)
FROM TBCSPIIT006M WHERE BAI_ORG_CD = #INST_CD# AND ABLT_YN_CD = '0') AS GCC_HHT_ORG_CD FROM TBDCMACM015M WHERE INST_CD = #INST_CD#