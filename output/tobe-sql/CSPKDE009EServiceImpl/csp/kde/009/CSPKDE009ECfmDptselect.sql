SELECT D1.REGI_SRNO AS REGI_SRNO /* 일련번호 */,
    D1.REG_YMD AS REG_YMD /* 등록일자 */,
    D1.REG_DEPT_CD AS REG_DEPT_CD /* 등록부서코드 */,
    F_DPT_INFO(D1.REG_DEPT_CD,'1') AS F_DPT_INFO /* 등록부서명 */,
    F_DPT_INFO(D1.REG_DEPT_CD,'4') AS F_DPT_INFO /* 약칭 등록부서명 */,
    D1.OTSD_EXPRT_RGTR_ENO AS OTSD_EXPRT_RGTR_ENO /* 외부전문가등록자직원전산번호 */,
    F_USR_INFO(D1.OTSD_EXPRT_RGTR_ENO,'2') AS F_USR_INFO /* 등록자전산번호 성명 */,
    D1.OGDP_INST_CD AS OGDP_INST_CD /* 소속기관코드 */,
    D1.TPOF_OGDP_INST_NM AS TPOF_OGDP_INST_NM /* 제보소속기관명 */,
    D1.CRU_OGDP_DEPT_NM AS CRU_OGDP_DEPT_NM /* 부패소속부서명 */,
    D1.TPOF_OGDP_INST_NM || '(' || D1.CRU_OGDP_DEPT_NM || ')' AS TPOF_OGDP_INST_NM /* 제보소속기관명 */,
    D1.TPOF_OGDP_INST_NM /* 제보소속기관명 */,
    D1.JBGD_NM AS JBGD_NM /* 직급명 */,
    D1.INFO_RLPR_NM AS INFO_RLPR_NM /* 정보관계자명 */,
    D1.CRU_RRNO_SCRTY AS CRU_RRNO_SCRTY /* 부패주민등록번호보안 */,
    D1.CFM_DDLN_DT AS CFM_DDLN_DT /* 확인기한일 */,
    D1.IDNTY_YN AS IDNTY_YN /* 확인여부 */,
    D2.SVY_FACT_NTF_DT AS SVY_FACT_NTF_DT /* 조사사실통보일자 */,
    D2.SVY_FACT_NTF_CNB AS SVY_FACT_NTF_CNB /* 조사사실통보전산번호 */,
    F_DPT_INFO(F_USR_INFO(D2.SVY_FACT_NTF_CNB,'5'),'1') AS F_DPT_INFO /* 조사사실통보부서명 */,
    F_USR_INFO(D2.SVY_FACT_NTF_CNB,'2') AS F_USR_INFO /* 조사사실통보이름 */,
    D2.CFM_DPT_CD AS CFM_DPT_CD /* 확인부서 */,
    CASE WHEN D2.CFM_DT_1 IS NOT NULL THEN '과장확인 : ' || F_USR_INFO(D2.CFM_CNB_1,'2') || '['|| TO_CHAR(TO_DATE(CFM_DT_1),'YYYY-MM-DD') || ']' ELSE '' END AS CFM_DT_1 /* 과장확인내용 */,
    CASE WHEN D2.CFM_DT_2 IS NOT NULL THEN '담당확인 : ' || F_USR_INFO(D2.CFM_CNB_2,'2') || '['|| TO_CHAR(TO_DATE(CFM_DT_2),'YYYY-MM-DD') || ']' ELSE '' END AS CFM_DT_2 /* 담당확인내용 */,
    D2.CFM_CNB_1 AS CFM_CNB_1 /* 확인전산번호1(과장) */,
    D2.CFM_DT_1 AS CFM_DT_1 /* 확인일1(과장) */,
    D2.CFM_CNB_2 AS CFM_CNB_2 /* 확인전산번호2(담당자) */,
    D2.CFM_DT_2 AS CFM_DT_2 /* 확인일(담당자) */,
    CASE WHEN D2.CFM_DT_1 IS NULL THEN 'N' ELSE 'Y' END AS CFM_DT_1 /* 과장확인여부 */,
    CASE WHEN D2.CFM_DT_2 IS NULL THEN 'N' ELSE 'Y' END AS CFM_DT_2 /* 담당확인여부 */,
    CASE WHEN D1.CVLCPT_DOC_ID IS NOT NULL THEN '파일첨부' ELSE D1.CRU_RRNO_SCRTY END AS CVLCPT_DOC_ID /* 민원문서아이디 */,
    D1.CVLCPT_DOC_ID AS CVLCPT_DOC_ID /* 민원문서아이디 */
FROM TBCSPKDE005M D1 /* 의원면직기본 */ ,TBCSPKDE006L D2 /* 의원면직확인내역 */ WHERE 1=1 AND D1.REGI_SRNO = D2.REGI_SRNO AND D1.REGI_SRNO = #strRegiSrno# AND D2.CFM_DPT_CD = #strCfmDptCd#