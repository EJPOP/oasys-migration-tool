SELECT FRS_MDM_SQNO /* 포렌식매체순번 */,
    MTRL_DVSN_CD,
    F_CODE_NM('1000864',MTRL_DVSN_CD) AS F_CODE_NM,
    SRECS_DEPT_CD AS SRECS_DEPT_CD /* 심사재심의부서코드 */,
    F_DPT_INFO(SRECS_DEPT_CD,'1') AS CNSTN_MBCMT_DEPT_NM /* 자문위원부서명 */,
    RSCH_ASMT_TTL /* 연구과제제목 */,
    DTL_TXT,
    CVLCPT_DOC_ID /* 민원문서아이디 */,
    OTSD_EXPRT_RGTR_ENO /* 외부전문가등록자직원전산번호 */,
    F_USR_INFO(OTSD_EXPRT_RGTR_ENO,'2') AS RGTR_NM /* 등록자명 */,
    TO_CHAR(FRST_REG_DT, 'YYYY-MM-DD') AS TO_CHAR
FROM TBFAAAEA045M WHERE MTRL_DVSN_CD = #strMtrlDvsnCd# AND SRECS_DEPT_CD IN (SELECT SRECS_DEPT_CD /* 심사재심의부서코드 */
FROM TABLE(F_AUTH_DEPT(#strCnb#, #strDptAuth#, '', #strDptCd#))) AND SRECS_DEPT_CD = #SRECS_DEPT_CD# AND RSCH_ASMT_TTL LIKE '%'||#strTitNm#||'%'