SELECT MAX (DECODE (CD, 1, CD_NM, 0) ) AS CD,
    MAX (DECODE (CD, 1, USR_DFN_TXT_10, 0) ) AS CD,
    MAX (DECODE (CD, 2, CD_NM, 0) ) AS CD,
    MAX (DECODE (CD, 2, USR_DFN_TXT_10, 0) ) AS CD
FROM TBDCMACM013C WHERE 1=1 AND CMM_CD_DVSN_CD='1000898' AND CD != '000000000' GROUP BY CMM_CD_DVSN_CD