SELECT D1.CLMETG_SRNO AS CLMETG_SRNO /* 소집일련번호 */,
    D1.APOLS_DT AS APOLS_DT /* 응소일 */,
    D1.OFOD_HM AS OFOD_HM /* 발령시분 */,
    D1.APOLS_HM AS APOLS_HM /* 응소시분 */,
    D1.CLSNG_HM AS CLSNG_HM /* 마감시분 */,
    D1.CLMETG_TIT_TXT AS CLMETG_TIT_TXT /* 소집제목 */,
    (SELECT CASE WHEN COUNT(1) > 0 THEN '' ELSE '소집대상자 생성' AS 1
FROM TBCSPOEP003L WHERE CLMETG_SRNO = D1.CLMETG_SRNO) AS CLMETG_TG_MAKE ,(SELECT COUNT(1)
FROM TBCSPOEP003L WHERE CLMETG_SRNO = D1.CLMETG_SRNO) AS CLMETG_TG_CNT FROM TBCSPOEP002M D1 ORDER BY D1.CLMETG_SRNO DESC