SELECT SRECS_DEPT_CD /* 심사재심의부서코드 */,
    CNSTN_MBCMT_DEPT_NM /* 자문위원부서명 */,
    HIRK_DPT_CD
FROM TBDCMACM002M T1 WHERE 1=1 AND SRECS_DEPT_CD LIKE '%000' AND USE_YN = 'Y' AND SRECS_DEPT_CD IN ( SELECT SUBSTR(SRECS_DEPT_CD,'1','3')||'000' AS SRECS_DEPT_CD /* 심사재심의부서코드 */
FROM TABLE(F_AUTH_DEPT(#{ssCnb}, NVL(#{srchAuthCd},'DEPTLW'), 'Y'))) ORDER BY DPT_SNO