SELECT TOTAL_COUNT,
    RNUM,
    GCC_ORG_CD,
    BAI_ORG_CD,
    ALL_ORG_NM,
    LOWT_ORG_NM,
    ORG_DGE_DVSN_CD
FROM ( SELECT INLINE.*,
    ROWNUM,
    COUNT(*) OVER() AS OVER
FROM ( SELECT GCC_ORG_CD,
    BAI_ORG_CD,
    ALL_ORG_NM,
    LOWT_ORG_NM,
    ORG_DGE_DVSN_CD
from UBCREOWN.TBCSPIIT006M WHERE NVL(ORG_DGE_DVSN_CD,0) > 1 AND NVL(ORG_DGE_DVSN_CD,0) ALL_ORG_NM LIKE '%' || #submit_org_name# || '%' (BAI_ORG_CD IS NULL OR BAI_ORG_CD LIKE '%' || #submit_parent_org_code# || '%') ORDER BY ALL_ORG_NM ) INLINE ) LIST WHERE RNUM BETWEEN #start_no# AND #end_no# ORDER BY RNUM ASC