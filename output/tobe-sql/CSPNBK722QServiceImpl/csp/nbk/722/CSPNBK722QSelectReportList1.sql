/* * 잔액이 있는 출자자들의 리스트 */ SELECT A.MOUI_NO,
    CASE B.MOUI_DVSN_CD WHEN '0' THEN C.USER_NM ELSE B.OGNZ_NM END AS MOUI_DVSN_CD,
    A.DEAL_AF_LEDG_BL
FROM TBCSPNBK102L A ,TBCSPNBK001M B , TBDCMACM001M C WHERE (A.MOUI_NO, A.INV_DEAL_DT, A.INV_DEAL_SLN) IN ( SELECT A.MOUI_NO,
    A.INV_DEAL_DT,
    MAX(INV_DEAL_SLN)
FROM TBCSPNBK102L A WHERE (A.MOUI_NO, A.INV_DEAL_DT) IN ( SELECT A.MOUI_NO,
    MAX(B.INV_DEAL_DT)
FROM TBCSPNBK001M A, TBCSPNBK102L B WHERE B.INV_DEAL_DT 0 AND B.SCS_YN = 'N' ORDER BY A.MOUI_NO