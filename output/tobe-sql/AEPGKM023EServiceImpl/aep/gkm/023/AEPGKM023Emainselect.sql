SELECT EVL_CLU_CD,
    EVL_CLU_NM,
    EVL_TXT,
    NVL(MIN_SC, 0) AS MIN_SC,
    MAX_SC,
    USE_YN /* 사용여부 */,
    (SELECT CASE WHEN COUNT(1) > 0 THEN 'N' ELSE 'Y' AS 1
FROM TBAEPGKM010L WHERE EVL_CLU_CD = A.EVL_CLU_CD) AS DML_YN FROM TBAEPGKM008M AS A WHERE 1=1 USE_YN = #USE_YN# ORDER BY EVL_CLU_CD