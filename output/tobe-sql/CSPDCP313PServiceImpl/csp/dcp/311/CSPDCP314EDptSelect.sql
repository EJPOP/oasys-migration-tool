SELECT SRECS_DEPT_CD /* 심사재심의부서코드 */,
    CNSTN_MBCMT_DEPT_NM /* 자문위원부서명 */
FROM TBDCMACM002M WHERE SRECS_DEPT_CD LIKE '071%' AND USE_YN ='Y' AND SRECS_DEPT_CD NOT IN ('071000') UNION SELECT SRECS_DEPT_CD /* 심사재심의부서코드 */,
    CNSTN_MBCMT_DEPT_NM /* 자문위원부서명 */
FROM TBDCMACM002M WHERE SRECS_DEPT_CD LIKE '062%' AND USE_YN ='Y' AND SRECS_DEPT_CD NOT IN ('062000')