SELECT CRU_ZIP AS CRU_ZIP /* 부패우편번호 */,
    TRIM(OTSD_EXPRT_ADDR) AS OTSD_EXPRT_ADDR /* 외부전문가주소 */,
    NVL(TRIM(CRU_CVLPR_DADDR), ' ') AS CRU_CVLPR_DADDR /* 부패민원인상세주소 */,
    NVL(TRIM(SRECS_CLMT_NM),' ') AS SRECS_CLMT_NM /* 심사재심의청구인명 */
FROM TB_BADFRE012M WHERE 1=1 AND (OTSD_EXPRT_ADDR <> '' OR OTSD_EXPRT_ADDR <> ' ' OR OTSD_EXPRT_ADDR IS NOT NULL) AND CVLCPT_RCPT_YR = #CVLCPT_RCPT_YR# AND SRECS_RCPT_SECD = #SRECS_RCPT_SECD# AND SRECS_DEPT_CD = #SRECS_DEPT_CD# SRECS_RCPT_SQNO = #srnoArr[]#