SELECT D1.CD,
    D1.CD_NM
FROM (SELECT CD,
    CD_NM,
    OTPT_SEQ
FROM TBDCMACM013C WHERE CMM_CD_DVSN_CD = '1000272' AND CD NOT IN ('000000000', '10000') AND USE_YN = 'Y' AND CD = #strTskKndCd# AND CD_NM like #strCdNm# || '%' AND CD_NM like #strCdNm# || '%' ) D1 , (SELECT AUD_TSK_KND_CD
FROM TBCSPAOE029M WHERE 1=1 AND EVL_YR = #strEvlYr# AND HLYR_DVSN_CD = #strHlyrDvsnCd# AND CVLCPT_TASK_SECD = '10001' /*감사외업무관리*/ AND EVL_STEP_CD = '700' /*확인자*/ AND EVL_DTM_YN = 'Y' /*평가완료*/ GROUP BY AUD_TSK_KND_CD ) D2 WHERE D1.CD = D2.AUD_TSK_KND_CD ORDER BY SUBSTR(CD_NM, 0, 4), OTPT_SEQ, CD