SELECT /* csp.bii.service.impl.CSPBII111QServiceImpl.CSPBII111QMlgInfSelect */ AA.SBMSN_WHOL_SCR + AA.SBMSN_ADD_WHOL_SCR + AA.UTLZ_ADD_WHOL_SCR + AA.CRFW_SCR + AA.GNRL_ADSC_WHOL_CONT + AA.DEPT_INFO_WHOL_SCR + AA.QLT_ADSC_WHOL_CONT + AA.MLG_ADD_WHOL_NOCS + AA.CRFW_QLT_SCR AS MLG_TSC_CONT /* 마일리지총점개수 */,
    AA.SBMSN_WHOL_SCR + AA.SBMSN_ADD_WHOL_SCR + AA.SPCF_INFO_ADD_WHOL_SCR + AA.GNRL_ADSC_WHOL_CONT AS SBMSN_WHOL_SCR /* 제출전체점수 */,
    AA.CRFW_SCR /* 이월점수 */,
    AA.SBMSN_WHOL_SCR /* 제출전체점수 */,
    AA.SPCF_INFO_ADD_WHOL_SCR /* 특정정보추가전체점수 */,
    AA.CTGRY_ADD_TSC /* 범주추가총점 */,
    AA.SPCF_INFO_ADD_WHOL_SCR /* 특정정보추가전체점수 */,
    AA.UTLZ_ADD_WHOL_SCR /* 활용추가전체점수 */,
    AA.PRCS_ADD_TSC_CONT /* 처리추가총점개수 */,
    AA.GNRL_ADSC_WHOL_CONT /* 일반가산점전체개수 */,
    AA.DEPT_INFO_WHOL_SCR /* 부서정보전체점수 */,
    AA.QLT_ADSC_WHOL_CONT /* 품질가산점전체개수 */,
    AA.MLG_ADD_WHOL_NOCS /* 마일리지추가전체건수 */,
    AA.QTR_GOAL_SCR /* 분기목표점수 */,
    AA.SBMSN_NOCS /* 제출건수 */,
    nvl(AA.SMT_SUS_NOC,0) AS SMT_SUS_NOC /* 인정건수 */,
    AA.CRFW_QLT_SCR /* 이월품질점수 */
FROM ( SELECT nvl(A.CRFW_SCR,0) AS CRFW_SCR /* 이월점수 */,
    nvl(A.CRFW_QLT_SCR,0) AS CRFW_QLT_SCR /* 이월품질점수 */,
    nvl(A.SBMSN_WHOL_SCR,0) AS SBMSN_WHOL_SCR /* 제출전체점수 */,
    nvl(A.SBMSN_ADD_WHOL_SCR,0) AS SBMSN_ADD_WHOL_SCR /* 제출추가전체점수 */,
    nvl(A.SPCF_INFO_ADD_WHOL_SCR,0) AS SPCF_INFO_ADD_WHOL_SCR /* 특정정보추가전체점수 */,
    nvl(A.CTGRY_ADD_TSC,0) AS CTGRY_ADD_TSC /* 범주추가총점 */,
    nvl(A.UTLZ_ADD_WHOL_SCR,0) AS UTLZ_ADD_WHOL_SCR /* 활용추가전체점수 */,
    nvl(A.PRCS_ADD_TSC_CONT,0) AS PRCS_ADD_TSC_CONT /* 처리추가총점개수 */,
    nvl(A.GNRL_ADSC_WHOL_CONT,0) AS GNRL_ADSC_WHOL_CONT /* 일반가산점전체개수 */,
    nvl(A.DEPT_INFO_WHOL_SCR,0) AS DEPT_INFO_WHOL_SCR /* 부서정보전체점수 */,
    nvl(A.QLT_ADSC_WHOL_CONT,0) AS QLT_ADSC_WHOL_CONT /* 품질가산점전체개수 */,
    nvl(A.MLG_ADD_WHOL_NOCS,0) AS MLG_ADD_WHOL_NOCS /* 마일리지추가전체건수 */,
    nvl(A.QTR_GOAL_SCR,0) AS QTR_GOAL_SCR /* 분기목표점수 */,
    nvl(A.SBMSN_NOCS,0) AS SBMSN_NOCS /* 제출건수 */,
    (SELECT count(*)
FROM TBCSPBII100M M WHERE M.PRSNR_ENO = #TRGT_NOP_ENO# AND M.ADT_INFO_PRGRS_STTS_SECD IN ('15','99') AND CASE WHEN M.ADT_INFO_PRGRS_STTS_SECD IN ('01','02','03','04') THEN 1 WHEN M.SBMSN_YMD >= #strStrDt# THEN 1 ELSE 0 END = 1 AND CASE WHEN M.ADT_INFO_PRGRS_STTS_SECD IN ('01','02','03','04') THEN 1 WHEN M.SBMSN_YMD = '05' AND F_CODE_NM('1000039', M.PRCS_DRCTN_SECD) NOT LIKE '%폐기%' ) AS SMT_SUS_NOC FROM TBCSPBII160L A WHERE A.INFO_YR = REGEXP_SUBSTR(#strYeQrDvsnCd#, '[^|]+', 1, 1) AND A.INFO_MLG_QTR_SECD = REGEXP_SUBSTR(#strYeQrDvsnCd#, '[^|]+', 1, 2) AND A.TRGT_NOP_ENO = #TRGT_NOP_ENO# ) AA