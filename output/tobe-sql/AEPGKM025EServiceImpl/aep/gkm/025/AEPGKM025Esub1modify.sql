DECLARE BEGIN DELETE FROM TBAEPGKM012L WHERE EVL_YR = #EVL_YR# AND EVL_DVSN_CD = #EVL_DVSN_CD#; INSERT INTO TBAEPGKM012L (SC_GRN_TG_CD /* 점수부여대상코드 */, EVL_YR /* 평가년도 */, EVL_DVSN_CD /* 평가구분코드 */, FRST_USER_ID /* 최초사용자ID */, LAST_USER_ID /* 최종사용자ID */, LAST_PRCS_DEPT_CD /* 최종거래부서코드 */, LAST_MENU_ID /* 최종메뉴ID */, FRST_REG_DT /* 최초등록일시 */, LAST_MDFCN_DT /* 최종수정일시 */) SELECT SC_GRN_TG_CD,
    #EVL_YR# AS #EVL_YR#,
    #EVL_DVSN_CD# AS #EVL_DVSN_CD#,
    #FRT_USR_ID# AS FRST_USER_ID /* 최초사용자아이디 */,
    #FNL_USR_ID# AS LAST_USER_ID /* 최종사용자아이디 */,
    #FNL_DEAL_DPT_CD# AS LAST_PRCS_DEPT_CD /* 최종처리부서코드 */,
    #FNL_MNU_ID# AS LAST_MENU_ID /* 최종메뉴아이디 */,
    SYSDATE AS FRST_REG_DT,
    SYSDATE AS LAST_MDFCN_DT
FROM TBAEPGKM006M WHERE USE_YN = 'Y'; END;