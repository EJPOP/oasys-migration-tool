SELECT CASE A.MOUI_DVSN_CD WHEN '0' THEN C.USER_NM ELSE A.OGNZ_NM END AS MOUI_DVSN_CD,
    A.MOUI_NO,
    B.DEAL_AF_LEDG_BL,
    (SELECT AS SET_TXT
FROM TBCSPNBK003M WHERE OP_MTRL_MNG_CD = '4') AS NM, #strDT# AS LEND_DEAL_DT FROM TBCSPNBK001M A , TBCSPNBK102L B , TBDCMACM001M C WHERE A.MOUI_NO = B.MOUI_NO AND A.TRGT_NOP_ENO = C.TRGT_NOP_ENO AND A.MOUI_NO = #MOUI_NO# AND B.INV_DEAL_DT= ( SELECT MAX(INV_DEAL_DT)
FROM TBCSPNBK102L WHERE MOUI_NO = #MOUI_NO# AND INV_DEAL_DT <= #strDT# ) AND B.INV_DEAL_SLN = ( SELECT MAX(INV_DEAL_SLN)
FROM TBCSPNBK102L WHERE MOUI_NO = #MOUI_NO# AND INV_DEAL_DT = ( SELECT MAX(INV_DEAL_DT)
FROM TBCSPNBK102L WHERE MOUI_NO = #MOUI_NO# AND INV_DEAL_DT <= #strDT# ) )